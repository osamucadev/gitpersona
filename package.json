{
  "name": "gitpersona",
  "private": true,
  "version": "0.1.0",
  "description": "Manage multiple Git identities via a polished desktop UI",
  "scripts": {
    "dev": "npm run dev --workspace=apps/desktop",
    "build": "npm run build --workspace=apps/desktop",
    "tauri:dev": "npm run tauri:dev --workspace=apps/desktop",
    "tauri:build": "npm run tauri:build --workspace=apps/desktop",
    "lint": "npm run lint --workspace=apps/desktop",
    "test": "npm run test --workspace=apps/desktop"
  },
  "workspaces": [
    "apps/desktop"
  ]
}
